import { Component } from '@angular/core';

/**
 * Componente vacío usado para rutas que solo redirigen vía guards
 */
@Component({
  selector: 'app-redirect',
  template: '',
  standalone: true
})
export class RedirectComponent {}
